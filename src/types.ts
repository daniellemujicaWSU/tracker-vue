export interface CapacityCard {
    cardHeader: string,
    columnHeaders: string[],
    columnData: string[]
}

export interface StudentCard {
    profilePhoto: string,
    name: string,
    editStudent: string,
    outCard: string[],
    inCard: string[],
    typeCard: string[]
}
